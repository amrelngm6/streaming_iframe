
                    <div class="w-full p-4 lg:p-8 bg-white dark:bg-gray-900 text-primary rounded-xl">

                        <h2 class="text-2xl font-normal dark:text-gray-300">{{lang.translate('Trending Audio')}}</h2>
                        <hr class=" dark:opacity-30 my-2" />
                        
                        <div class="grid lg:grid-cols-4 md:grid-cols-3 sm:grid-cols-2 grid-cols-1 gap gap-4" x-data="{is_slide: ''}">
                            {% for media_item in explore_items.items %}
                            {% set list = explore_items %}
                            {% include 'views/front/'~ template ~ '/includes/track_item.html.twig' %}
                            {% endfor %}
                            
                        </div>
                    </div>