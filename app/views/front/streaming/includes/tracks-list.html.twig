
                                        <li class="relative" >
                                            <div class="py-1 lg:gap-4 gap-2 flex text-primary transition " >
                                                
                                                <img src="/stream?thumbnail=300&image={{media_item.picture_name}}" width="48" height="48" class="rounded-lg lg:block hidden" />
                                                <div class="slide__audio js-audio start-player" id="media-{{media_item.media_id}}" data-list="{{list.items|column('player_object')|json_encode}}" data-index="{{loop.index - 1}}">
                                                    <div class="slide__audio-player"  data-title="{{media_item.name}}" data-poster="/stream?thumbnail=300&image={{media_item.picture_name}}" data-artist="{{media_item.artist.name}}" data-path="/stream_audio?audio={{media_item.main_file.filename}}" data-id="{{media_item.media_id}}" data-wave-id="wave-{{media_item.media_id}}" data-wave-overlay="wave-overlay{{media_item.media_id}}">
                                                    </div>
                                                    {% include 'views/front/'~ app.SystemSetting.template ~ '/includes/player-circle.html.twig' %}
                                                </div>
                                                <div class="w-full hover:text-rose-500 max-w-[40%] lg:max-w-[90%]">
                                                    <a href="/audio/{{media_item.media_id}}"  class="ajax-load block truncate max-w-96 text-lg font-normal transition hover:text-rose-500  dark:text-gray-300 dark:hover:text-rose-400">{{media_item.name}}</a>
                                                    <a href="/artist/{{media_item.author_id}}"  class="ajax-load text-gray-400 text-sm">{{media_item.artist.name}}</a>
                                                </div>

                                                <div class="w-6">
                                                    {% set item = media_item  %}
                                                    {% include 'views/front/'~ app.SystemSetting.template ~ '/includes/like.html.twig' %}
                                                </div>
            
                                                <div class="p-1 text-gray-500" x-text="convertToTime({{media_item.field.duration}})"></div>
                                                <div class="w-4 relative">
                                                    <span onclick="(function(){ return jQuery('#activeItemId').val('{{media_item.media_id}}'), jQuery('#activeItemPicture').val('{{media_item.picture_name}}'),  jQuery('#track-options-cover').css('background-image', 'url(/stream?thumbnail=600&image={{media_item.picture_name}})' ),  jQuery('#track-item-title').html('{{media_item.name}}'), jQuery('#track-options-popup').toggleClass('hidden'); })();return false;" class=" cursor-pointer dark:text-gray-100 leading-5 text-xl font-extrabold" >...</span>
                                                </div>
                                                
                                            </div>
                                            
                                            <div class="relative h-28 w-full hidden wave-frame" > 
                                                <img id="wave-{{media_item.media_id}}" class="w-full left-0 top-0 bottom-0 my-auto absolute" src="/stream?thumbnail=1000&dir=audio{{'/tmp' in item.main_file.path ? '/tmp' : ''}}&image={{media_item.main_file.wave}}" />
                                                <div id="wave-overlay{{media_item.media_id}}"  class="absolute w-full h-full top-0 right-0 dark:bg-gray-900 opacity-50 bg-white z-50"> </div>
                                            </div>

                                        </li>
                                                