                
                <div class="p-4 w-full text-primary ">
                    <div class="py-4 text-center mb-10">
                        <h1 class="px-6 text-2xl text-gray-800 dark:text-gray-100">{{item.name}} </h1>
                        <p class="px-6 text-primary">{{item.description}} </p>
                    </div>
                    <div id="channel-list"  class="w-full   py-4 gap-4" >
                        
                        <div class="w-full p-4 lg:p-8 bg-white dark:bg-gray-900 text-primary rounded-xl">

                            <h2 class="text-2xl font-normal dark:text-gray-300">{{lang.translate('Trnding Videos')}}</h2>
                            <hr class=" dark:opacity-30 my-6" />
                        
                            {% set customer = app.customer_auth() %}
                            {% for iframe in list.items %}
                            {% include 'views/front/' ~ template ~ '/pages/iframe/item.html.twig' %}
                            {% endfor %}
                        </div>
                    </div>
                </div>

				{% set view_items_limit = app.SystemSetting.view_iframe_limit %}
                {% include 'views/front/' ~ template ~ '/includes/pagination.html.twig' %}

                <div class="h-10"></div>