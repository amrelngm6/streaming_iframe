
            
                <div class="w-full lg:flex gap-6 dark:bg-gray-900 bg-white   p-8 rounded-lg shadow shadow-lg relative">

                    <div class="w-full relative  text-primary  rounded-lg shadow-md  ">
                        <div class="w-full  border-1 p-8">
                            <div class="w-full dark:text-gray-300 text-xl font-bold tracking-wide">{{lang.translate('Found similar Videos')}} {{app.params.title}}</div>
                        </div>
                        <hr class="opacity-30"/>
                        <div class="w-full lg:p-8 py-8 px-2">
                            <div class="w-full flex">
                                <div class="w-full ">
                                    <div class="grid lg:grid-cols-4 md:grid-cols-3 sm:grid-cols-2 grid-cols-1 gap gap-4" x-data="{is_slide: ''}">
                                        {% for media_item in list.items %}
                                        {% include 'views/front/'~ template ~ '/includes/video-item.html.twig' %}
                                        {% endfor %}
                                    </div>
                                    
                                </div>
                            </div>
            				{% set view_items_limit = app.SystemSetting.view_videos_limit %}
                            {% include 'views/front/' ~ template ~ '/includes/pagination.html.twig' %}

                        </div>
                    </div>
                </div>

         