
                    <div class="w-full p-4 lg:p-8 bg-white dark:bg-gray-900 text-primary rounded-xl">
                        <h2 class="text-2xl font-normal dark:text-gray-300">{{hook.field.subtitle ?? lang.translate('Trending Audio')}}</h2>
                        <hr class=" dark:opacity-30 my-2" />
                        
                        <div class="grid {{hook.field.desktop_view_limit < 1 ? ' lg:grid-cols-4 ' : ' lg:grid-cols-' ~ hook.field.desktop_view_limit }} {{hook.field.tablet_view_limit < 1 ? ' md:grid-cols-2 ' : ' md:grid-cols-' ~ hook.field.tablet_view_limit }}   {{hook.field.tablet_view_limit < 1 ? ' sm:grid-cols-2 ' : ' sm:grid-cols-' ~ hook.field.tablet_view_limit }} grid-cols-1 gap gap-4" x-data="{is_slide: ''}">
                            {% for media_item in list.items %}
                            {% include 'views/front/'~ app.SystemSetting.template ~ '/includes/video-item.html.twig' %}
                            {% endfor %}
                        </div>
                    </div>